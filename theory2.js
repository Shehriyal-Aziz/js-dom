// Name the types of nodes in the DOM tree.

// There are several types of nodes in the DOM, but the main ones are:

// 1. Element Node       → represents HTML tags (like <div>, <p>, <h1>)
// 2. Text Node          → represents the actual text inside elements
// 3. Attribute Node     → represents attributes of elements (like class, id, src)
// 4. Comment Node       → represents comments (<!-- like this -->)
// 5. Document Node      → represents the whole document itself (the root of the DOM tree)

// All of these together form the complete DOM structure.